@{
    ViewBag.Title = "Home Page";
}

@if (User.Identity.IsAuthenticated)
{
    <h1>Welcome @(User.Identity.IsAuthenticated ? HttpContext.Current.User.Identity.Name : "guest!")</h1>
    <a href="@Url.Action("SignOut", "Home")">Wyloguj się</a>
}
