
@{
    ViewBag.Title = "Details";
}

<h2>Opinie pracownika</h2>
<br />

<table class="table table-bordered table-striped">
    <tr>
        <th>Opis</th>
    </tr>
    @for (int i = 0; i < Model.Rows.Count; i++)
    {
        <tr>
            <td>@Model.Rows[i][0]</td>
        </tr>
    }
</table>

